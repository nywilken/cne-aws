#!/usr/bin/env ruby

require 'cne'

cne = Cne.new

case ARGV[0]
  when 'instances'
    if ARGV[1].empty? || ARGV[2].empty?
      puts "Usage: cne instances [appname] [environment]".colorize(:red)
      exit 1
    else
      cne.list_instances(ARGV[1], ARGV[2])
    end
end
